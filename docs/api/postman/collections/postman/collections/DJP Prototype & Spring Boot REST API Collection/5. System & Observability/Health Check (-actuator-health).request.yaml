$kind: http-request
name: Health Check (/actuator/health)
description: Returns Spring Boot application and database connection health
  status (`UP`/`DOWN`).
url: http://localhost:8081/actuator/health
method: GET
order: 1000
