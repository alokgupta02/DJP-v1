# DJPv1 BACKEND OPERATIONAL RULES

## Context Allocation
* Read `backend/.djp_state.md`, `backend/be-todo.md`, `docs/api/` contracts, and `docs/architecture/` specs before modifying microservices.
* Follow TDD flow: ensure QA test coverage passes before finalizing REST routes.

## Code Quality Standards
* Implement explicit exception handling via `@ControllerAdvice`.
* Document all public `/api/v1` endpoints with OpenAPI/Markdown specs.
