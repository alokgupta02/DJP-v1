# DJP PROTOTYPE BACKEND OPERATIONAL RULES

## Context Allocation
* Read `prototype/backend/be-todo.md`, `prototype/todo.md`, and `prototype/backend/.djp_state.md` before modifying API endpoints.
* Coordinate with `prototype/tests/` (QA Agent) to ensure `api-health.test.mjs` verifies every new or updated route.

## Code Quality Standards
* Ensure all endpoints under `/djp/api/v1/*` handle serialization cleanly without proxy errors.
* Keep `data.sql` seeded with all required non-nullable fields so H2 initializes reliably.
* Maintain strict alignment with `prototype/frontend/` requirements.
