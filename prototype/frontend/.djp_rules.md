# DJP PROTOTYPE FRONTEND OPERATIONAL RULES

## Context Allocation
* Refer to `prototype/frontend/fe-todo.md`, `prototype/todo.md`, and `prototype/frontend/.djp_state.md` before modifying UI code.
* Coordinate with `prototype/backend/` (BE Agent) and `prototype/tests/` (QA Agent) to ensure API routes and verification tests remain aligned.

## Code Quality Standards
* Use inline Tailwind utility classes.
* Ensure all API calls use `/djp/api/v1/...` relative paths so they route through Vite proxy cleanly during development.
* Keep explanations structured and verifiable.
